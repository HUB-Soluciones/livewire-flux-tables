{
    "name": "livewire-flux-tables",
    "private": true,
    "scripts": {
        "test:e2e:install": "playwright install chromium",
        "test:e2e": "composer build && playwright test",
        "test:e2e:ui": "composer build && playwright test --ui"
    },
    "devDependencies": {
        "@playwright/test": "^1.62.1"
    }
}
